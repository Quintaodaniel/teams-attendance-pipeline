# --- Estrutura de Dados (LGPD) ---
# Ignora o conteúdo das pastas, mas permite que a estrutura exista no repo
data/raw/*
data/processed/*
data/final/*

# Não ignore arquivos .gitkeep (isso mantém as pastas vivas no Git)
!**/.gitkeep

# --- Ambientes Virtuais ---
# Ignora pastas de bibliotecas instaladas (venv, .venv, env)
venv/
.venv/
env/
bin/
lib/
share/

# --- Python (Arquivos temporários) ---
# Ignora cache de execução do Python
__pycache__/
*.py[cod]
*$py.class

# --- IDEs ---
# Ignora configurações específicas do editor (VS Code, PyCharm)
.vscode/
.idea/

# --- Sistema Operacional ---
.DS_Store
Thumbs.db